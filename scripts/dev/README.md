# 开发工具脚本

本目录包含开发环境设置和检查脚本。

## 📋 脚本说明

### setup.sh

**功能**: 一键设置开发环境

**功能**:
- 检查 Go 版本
- 安装 Go 依赖
- 安装开发工具（Air、Wire、Ent、oapi-codegen、golangci-lint）
- 检查 Docker（可选）
- 生成代码
- 运行测试

**使用**:
```bash
bash scripts/dev/setup.sh
```

### check-env.sh

**功能**: 检查开发环境配置

**检查项**:
- 必需工具（Go、Git）
- 开发工具（Air、Wire、Ent 等）
- 可选工具（Docker）
- Go 版本
- 项目文件
- 依赖验证

**使用**:
```bash
bash scripts/dev/check-env.sh
```

## 🚀 快速开始

### 首次设置

```bash
# 运行设置脚本
bash scripts/dev/setup.sh
```

### 检查环境

```bash
# 检查开发环境
bash scripts/dev/check-env.sh
```

## 📝 相关命令

也可以通过 Makefile 使用：

```bash
# 安装开发工具
make install-tools

# 检查代码
make check

# 提交前检查
make pre-commit
```
