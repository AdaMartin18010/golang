# 跨维度知识关联 (Cross-Dimensional References)

## 知识图谱

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         知识关联图谱                                         │
└─────────────────────────────────────────────────────────────────────────────┘

形式理论 (FT) ─────────────────────────────────────► 工程实现 (EC)
     │                                                   │
     │ 理论基础                                           │ 生产实践
     │                                                   │
     ├─► FT-002 GMP调度器 ───────────────────────────► EC-042 任务调度器
     │                                                   │
     ├─► FT-015 并发模型 ─────────────────────────────► EC-008 熔断器
     │                                                   │
     ├─► FT-018 状态机理论 ───────────────────────────► EC-112 Saga模式
     │                                                   │
     └─► FT-020 一致性模型 ───────────────────────────► EC-111 事件溯源
                                                         │
语言设计 (LD) ─────────────────────────────────────► 工程实现 (EC)
     │
     ├─► LD-015 并发模式 ─────────────────────────────► EC-007 优雅关闭
     │
     ├─► LD-022 Context设计 ──────────────────────────► EC-066 Context传播
     │
     └─► LD-030 错误处理 ─────────────────────────────► EC-119 幂等性保证

技术栈 (TS) ◄──────────────────────────────────────► 工程实现 (EC)
     │
     ├─► TS-012 PostgreSQL ──────────────────────────► EC-065 事务隔离
     │
     ├─► TS-023 Redis ───────────────────────────────► EC-091 分布式锁
     │
     ├─► TS-034 Kafka ───────────────────────────────► EC-061 任务队列
     │
     └─► TS-045 etcd ────────────────────────────────► EC-116 分布式协调
```

## 学习路径推荐

### 路径 1：任务调度系统专家

```
FT-002 (GMP调度器)
    ↓
EC-042 (任务调度器架构)
    ↓
EC-109 (生产级实现)
    ↓
EC-099 (K8s CronJob) ──► EC-100 (Temporal)
```

### 路径 2：分布式系统可靠性

```
FT-020 (一致性模型)
    ↓
EC-008 (熔断器) + EC-007 (优雅关闭)
    ↓
EC-112 (Saga模式) + EC-111 (事件溯源)
    ↓
EC-119 (幂等性) + EC-113 (CRDT)
```

### 路径 3：云原生基础设施

```
EC-003 (Kubernetes基础)
    ↓
EC-099 (K8s CronJob) + EC-116 (etcd协调)
    ↓
EC-110 (资源配额) + EC-118 (背压控制)
    ↓
EC-100 (Temporal工作流)
```

## 关键依赖关系

| 文档 | 依赖前置知识 | 后续延伸 |
|------|-------------|---------|
| EC-109 | FT-002, LD-015 | EC-110, EC-111 |
| EC-112 | FT-018, EC-008 | EC-100 |
| EC-099 | EC-003, TS-045 | EC-100 |
| EC-116 | TS-045, FT-020 | EC-091 |
