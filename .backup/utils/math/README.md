# 数学工具

**版本**: v1.0
**更新日期**: 2025-11-11
**适用于**: Go 1.25.3

---

## 📋 目录

- [数学工具](#数学工具)
  - [📋 目录](#-目录)
  - [1. 概述](#1-概述)
  - [2. 功能特性](#2-功能特性)
  - [3. 使用示例](#3-使用示例)

---

## 1. 概述

数学工具提供了常用的数学计算功能，包括最大值、最小值、求和、平均值、绝对值、取整、范围限制、随机数生成、质数判断、斐波那契数列、百分比计算、线性插值、距离计算、三角函数等。

---

## 2. 功能特性

### 2.1 基础运算

- `Max`: 返回两个整数中的较大值
- `MaxInt64`: 返回两个int64中的较大值
- `MaxFloat64`: 返回两个float64中的较大值
- `Min`: 返回两个整数中的较小值
- `MinInt64`: 返回两个int64中的较小值
- `MinFloat64`: 返回两个float64中的较小值
- `MaxInts`: 返回整数切片中的最大值
- `MinInts`: 返回整数切片中的最小值
- `Sum`: 计算整数切片的总和
- `SumFloat64`: 计算float64切片的总和
- `Average`: 计算整数切片的平均值
- `AverageFloat64`: 计算float64切片的平均值

### 2.2 绝对值

- `Abs`: 返回整数的绝对值
- `AbsInt64`: 返回int64的绝对值
- `AbsFloat64`: 返回float64的绝对值

### 2.3 幂和根

- `Pow`: 计算x的y次方
- `Sqrt`: 计算平方根

### 2.4 取整

- `Ceil`: 向上取整
- `Floor`: 向下取整
- `Round`: 四舍五入
- `RoundTo`: 四舍五入到指定小数位

### 2.5 范围限制

- `Clamp`: 将值限制在[min, max]范围内
- `ClampFloat64`: 将值限制在[min, max]范围内
- `IsInRange`: 检查值是否在范围内
- `IsInRangeFloat64`: 检查值是否在范围内

### 2.6 随机数

- `RandomInt`: 生成指定范围内的随机整数
- `RandomFloat64`: 生成指定范围内的随机浮点数

### 2.7 数论

- `GCD`: 计算最大公约数
- `LCM`: 计算最小公倍数
- `Factorial`: 计算阶乘
- `IsPrime`: 检查是否为质数
- `NextPrime`: 返回下一个质数
- `Fibonacci`: 计算斐波那契数列的第n项

### 2.8 百分比

- `Percent`: 计算百分比
- `PercentOf`: 计算某个值占另一个值的百分比
- `PercentChange`: 计算百分比变化

### 2.9 插值

- `Lerp`: 线性插值
- `InverseLerp`: 反向线性插值
- `Remap`: 重新映射值到新范围

### 2.10 距离计算

- `Distance`: 计算两点之间的距离
- `Distance3D`: 计算三维空间中两点之间的距离

### 2.11 角度转换

- `DegToRad`: 角度转弧度
- `RadToDeg`: 弧度转角度

### 2.12 三角函数

- `Sin`: 正弦函数
- `Cos`: 余弦函数
- `Tan`: 正切函数

### 2.13 对数

- `Log`: 自然对数
- `Log10`: 以10为底的对数
- `Exp`: 自然指数

### 2.14 取模

- `Mod`: 取模运算
- `ModInt`: 整数取模运算

### 2.15 奇偶判断

- `IsEven`: 检查是否为偶数
- `IsOdd`: 检查是否为奇数
- `IsDivisible`: 检查是否可整除

---

## 3. 使用示例

### 3.1 基础运算

```go
import "github.com/yourusername/golang/pkg/utils/math"

// 最大值和最小值
max := math.Max(3, 5)  // 5
min := math.Min(3, 5)  // 3

// 切片中的最大值和最小值
nums := []int{1, 5, 3, 9, 2}
maxNum := math.MaxInts(nums)  // 9
minNum := math.MinInts(nums)  // 1

// 求和和平均值
sum := math.Sum(nums)  // 20
avg := math.Average(nums)  // 4.0
```

### 3.2 绝对值和取整

```go
// 绝对值
abs := math.Abs(-5)  // 5

// 取整
ceil := math.Ceil(3.7)  // 4.0
floor := math.Floor(3.7)  // 3.0
round := math.Round(3.7)  // 4.0
roundTo := math.RoundTo(3.14159, 2)  // 3.14
```

### 3.3 范围限制

```go
// 限制值在范围内
clamped := math.Clamp(10, 0, 5)  // 5
clamped = math.Clamp(-5, 0, 5)   // 0
clamped = math.Clamp(3, 0, 5)    // 3

// 检查值是否在范围内
inRange := math.IsInRange(3, 0, 5)  // true
```

### 3.4 随机数

```go
// 生成随机整数
randomInt := math.RandomInt(1, 100)  // 1-100之间的随机整数

// 生成随机浮点数
randomFloat := math.RandomFloat64(0.0, 1.0)  // 0.0-1.0之间的随机浮点数
```

### 3.5 数论

```go
// 最大公约数和最小公倍数
gcd := math.GCD(48, 18)  // 6
lcm := math.LCM(12, 18)  // 36

// 阶乘
fact := math.Factorial(5)  // 120

// 质数判断
isPrime := math.IsPrime(7)  // true
nextPrime := math.NextPrime(10)  // 11

// 斐波那契数列
fib := math.Fibonacci(10)  // 55
```

### 3.6 百分比

```go
// 计算百分比
percent := math.Percent(25, 100)  // 25.0

// 计算百分比变化
change := math.PercentChange(100, 120)  // 20.0
```

### 3.7 插值

```go
// 线性插值
lerped := math.Lerp(0, 10, 0.5)  // 5.0

// 反向线性插值
t := math.InverseLerp(0, 10, 5)  // 0.5

// 重新映射
remapped := math.Remap(5, 0, 10, 0, 100)  // 50.0
```

### 3.8 距离计算

```go
// 计算两点之间的距离
dist := math.Distance(0, 0, 3, 4)  // 5.0

// 计算三维空间中两点之间的距离
dist3D := math.Distance3D(0, 0, 0, 3, 4, 5)  // 约7.07
```

### 3.9 角度转换

```go
// 角度转弧度
rad := math.DegToRad(90)  // π/2

// 弧度转角度
deg := math.RadToDeg(math.Pi)  // 180.0
```

### 3.10 三角函数

```go
// 三角函数
sin := math.Sin(math.Pi / 2)  // 1.0
cos := math.Cos(0)  // 1.0
tan := math.Tan(math.Pi / 4)  // 约1.0
```

### 3.11 对数

```go
// 对数
log := math.Log(math.E)  // 1.0
log10 := math.Log10(100)  // 2.0
exp := math.Exp(1)  // 约2.718
```

### 3.12 奇偶判断

```go
// 奇偶判断
isEven := math.IsEven(4)  // true
isOdd := math.IsOdd(5)  // true
isDivisible := math.IsDivisible(10, 5)  // true
```

### 3.13 完整示例

```go
package main

import (
    "fmt"
    "github.com/yourusername/golang/pkg/utils/math"
)

func main() {
    // 基础运算
    nums := []int{1, 5, 3, 9, 2}
    fmt.Printf("Max: %d\n", math.MaxInts(nums))
    fmt.Printf("Min: %d\n", math.MinInts(nums))
    fmt.Printf("Sum: %d\n", math.Sum(nums))
    fmt.Printf("Average: %.2f\n", math.Average(nums))

    // 范围限制
    fmt.Printf("Clamp: %d\n", math.Clamp(10, 0, 5))

    // 数论
    fmt.Printf("GCD: %d\n", math.GCD(48, 18))
    fmt.Printf("LCM: %d\n", math.LCM(12, 18))
    fmt.Printf("Factorial: %d\n", math.Factorial(5))
    fmt.Printf("Fibonacci: %d\n", math.Fibonacci(10))

    // 百分比
    fmt.Printf("Percent: %.2f%%\n", math.Percent(25, 100))

    // 距离计算
    fmt.Printf("Distance: %.2f\n", math.Distance(0, 0, 3, 4))
}
```

---

**更新日期**: 2025-11-11
