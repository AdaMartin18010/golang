# Domain Layer (领域层)

本目录包含框架层面的抽象接口，不包含具体业务领域模型。

## ⚠️ 重要说明

**本框架是框架性代码，不包含具体业务逻辑**。

如果存在 `user/`、`order/` 等目录，这些是**示例代码**，仅用于展示框架的使用方式。

## 框架抽象接口

- `interfaces/repository.go` - 通用仓储接口
- `interfaces/service.go` - 通用领域服务接口

## 使用方式

用户应该：
1. 通过 **Ent Schema** 定义自己的业务领域模型
2. 通过 **框架提供的接口** 实现自己的业务逻辑
3. 参考 `examples/framework-usage/` 中的示例代码
