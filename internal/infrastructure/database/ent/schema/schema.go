package schema

import (
	"entgo.io/ent"
)

// 导出所有 schema
// 注意：框架不提供具体的 Schema 定义
// 用户需要在自己的项目中定义 Ent Schema
var (
	Schemas = []ent.Interface{
		// 用户应该在这里添加自己的 Schema
		// 例如: &User{}, &Product{}, etc.
	}
)
