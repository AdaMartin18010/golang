# 框架代码与业务代码说明

> **版本**: v1.0
> **日期**: 2025-01-XX

---

## 📋 概述

本文档明确说明框架代码和业务代码的区别，确保框架保持纯净，不包含具体业务逻辑。

## 🎯 核心原则

### 框架代码（Framework Code）

**定义**: 提供基础设施、工具库、中间件系统的通用代码。

**特征**:
- ✅ 不依赖任何业务领域
- ✅ 可被任何业务使用
- ✅ 提供抽象接口和通用模式
- ✅ 技术栈集成和封装

**位置**:
- `pkg/` - 通用工具包
- `internal/framework/` - 框架基础设施
- `internal/domain/interfaces/` - 框架抽象接口
- `internal/application/patterns/` - 通用应用模式
- `internal/infrastructure/` - 技术栈实现
- `internal/interfaces/` - 接口适配层（HTTP、gRPC、GraphQL）

### 业务代码（Business Code）

**定义**: 实现具体业务逻辑的代码。

**特征**:
- ❌ 依赖特定业务领域
- ❌ 包含具体业务规则
- ❌ 实现具体业务用例

**不应在框架中的位置**:
- `internal/domain/user/` - 用户领域（业务代码）
- `internal/application/user/` - 用户应用服务（业务代码）
- `internal/interfaces/http/chi/handlers/user_handler.go` - 用户处理器（业务代码）

## 📁 目录结构说明

### ✅ 框架代码结构

```
internal/
├── domain/
│   └── interfaces/      # ✅ 框架抽象接口（Repository、Service）
├── application/
│   └── patterns/        # ✅ 通用应用模式（Command、Query、Event、DTO）
├── infrastructure/      # ✅ 技术栈实现（数据库、消息队列、缓存等）
└── interfaces/          # ✅ 接口适配层（HTTP、gRPC、GraphQL）
```

### ❌ 业务代码（应作为示例）

```
internal/
├── domain/
│   └── user/            # ❌ 业务代码（应移至 examples/）
├── application/
│   └── user/            # ❌ 业务代码（应移至 examples/）
└── interfaces/http/chi/handlers/
    └── user_handler.go  # ❌ 业务代码（应移至 examples/）
```

## 🔄 迁移建议

### 方案 1: 移动到示例目录（推荐）

将业务代码移动到 `examples/framework-usage/` 作为使用示例：

```bash
# 移动用户领域示例
mv internal/domain/user examples/framework-usage/user-domain/domain/
mv internal/application/user examples/framework-usage/user-domain/application/
mv internal/interfaces/http/chi/handlers/user_handler.go examples/framework-usage/user-domain/interfaces/http/
```

### 方案 2: 标记为示例代码

在代码中添加注释，明确说明这是示例代码：

```go
// Package user 提供用户领域示例
//
// ⚠️ 注意：这是示例代码，展示如何使用框架构建领域模型。
// 实际项目中，用户应该定义自己的业务领域模型。
//
// 示例位置：examples/framework-usage/user-domain/
package user
```

## 📝 检查清单

在添加新代码时，请检查：

- [ ] 代码是否依赖特定业务领域？
- [ ] 代码是否包含具体业务规则？
- [ ] 代码是否实现具体业务用例？

如果以上任何一项为"是"，则应该：
1. 将其作为示例代码放在 `examples/framework-usage/`
2. 或明确标记为示例代码

## 🎯 框架应该提供什么

### ✅ 应该提供

1. **抽象接口**
   - `Repository[T]` - 通用仓储接口
   - `Service` - 通用服务接口
   - `Command`、`Query`、`Event` - 应用模式

2. **技术栈实现**
   - 数据库连接和 ORM 集成
   - 消息队列客户端
   - 缓存客户端
   - 可观测性工具

3. **中间件和工具**
   - HTTP 中间件（认证、限流、追踪等）
   - 错误处理框架
   - 日志系统
   - 配置管理

### ❌ 不应该提供

1. **具体业务领域**
   - 用户管理
   - 订单管理
   - 产品管理

2. **具体业务逻辑**
   - 用户创建流程
   - 订单支付流程
   - 业务规则实现

## 📚 相关文档

- [项目定位与任务梳理](../00-项目定位与任务梳理.md)
- [框架使用示例说明](02-框架使用示例说明.md)
- [框架基础设施说明](00-框架基础设施说明.md)

---

**最后更新**: 2025-01-XX
