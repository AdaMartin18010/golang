# 框架基础设施说明

> **版本**: v1.1
> **日期**: 2025-01-XX
> **状态**: 持续完善中

---

## 📋 概述

本文档说明框架的基础设施组件，这些组件是框架的核心，为应用开发提供统一的基础能力。

## 🎯 设计原则

1. **框架性代码**：不包含具体业务逻辑，只提供基础设施
2. **统一接口**：所有组件提供统一的接口，便于使用和替换
3. **可配置**：支持灵活的配置，适应不同场景
4. **可测试**：所有组件支持测试，提供 Mock 能力
5. **文档完善**：每个组件都有详细的文档和示例

---

## 📦 核心组件

### 1. 统一日志系统

**位置**: `internal/framework/logger/`

**功能**:
- 统一的日志接口
- 结构化日志（基于 slog）
- OpenTelemetry 集成
- 日志级别、采样率配置
- 线程安全

**使用**:
```go
import "github.com/yourusername/golang/internal/framework/logger"

log := logger.GetLogger()
log.Info("Application started", "port", 8080)
```

**文档**: [internal/framework/logger/README.md](../../internal/framework/logger/README.md)

---

### 2. 本地开发循环（热重载）

**位置**: `.air.toml`

**功能**:
- 文件变化自动检测
- 自动重新编译和运行
- 快速开发反馈

**使用**:
```bash
# 安装 Air
go install github.com/cosmtrek/air@latest

# 开发模式运行
make run-dev
# 或
air
```

**配置**: `.air.toml`

---

### 3. 测试覆盖工具

**位置**: `codecov.yml`, `.github/workflows/ci.yml`, `scripts/test-coverage.sh`

**功能**:
- 测试覆盖率收集
- 覆盖率报告生成
- CI/CD 集成
- 覆盖率阈值检查

**使用**:
```bash
# 生成覆盖率报告
make test-coverage

# 查看覆盖率
go tool cover -html=coverage.out
```

**配置**:
- `codecov.yml`: Codecov 配置
- CI 工作流: 自动上传覆盖率报告

---

### 4. API 规范与代码生成

**位置**: `api/`, `scripts/api/`

**功能**:
- OpenAPI 3.1.0 规范支持
- AsyncAPI 3.0.0 规范支持
- 代码生成（服务器、客户端）
- 文档生成（HTML）
- 规范验证
- Swagger UI 集成

**使用**:
```bash
# 生成 OpenAPI 代码
make generate-openapi

# 验证 API 规范
make validate-api

# 生成 API 文档
make generate-api-docs
```

**文档**: [API规范与代码生成](01-API规范与代码生成.md)

---

### 5. 配置管理

**位置**: `internal/config/`

**功能**:
- 配置文件加载（YAML/JSON/ENV）
- 环境变量支持
- 配置热重载
- 默认值支持

**使用**:
```go
import "github.com/yourusername/golang/internal/config"

cfg, err := config.Load()
```

**文档**: [internal/config/README.md](../../internal/config/README.md)

---

### 6. 依赖注入

**位置**: `scripts/wire/`

**功能**:
- 编译时依赖注入
- 类型安全
- 零反射

**使用**:
```bash
# 生成依赖注入代码
make generate-wire
```

**文档**: [docs/architecture/00-架构模型与依赖注入完整说明.md](../architecture/00-架构模型与依赖注入完整说明.md)

---

## 🔧 开发工具

### Makefile 命令

```bash
make help              # 显示所有可用命令
make deps              # 安装依赖
make generate          # 生成代码（Ent, Wire, OpenAPI等）
make build             # 构建应用
make run               # 运行应用
make run-dev           # 开发模式运行（热重载）
make test              # 运行测试
make test-coverage     # 运行测试并生成覆盖率报告
make lint              # 代码检查
make fmt               # 格式化代码
make generate-openapi  # 生成 OpenAPI 代码
make generate-asyncapi # 生成 AsyncAPI 代码
make validate-api      # 验证所有 API 规范
make generate-api-docs # 生成 API 文档
```

### 开发流程

1. **安装依赖**
   ```bash
   make deps
   ```

2. **安装开发工具**
   ```bash
   # Air (热重载)
   go install github.com/cosmtrek/air@latest

   # Wire (依赖注入)
   go install github.com/google/wire/cmd/wire@latest

   # Ent (ORM)
   go install entgo.io/ent/cmd/ent@latest

   # oapi-codegen (OpenAPI 代码生成)
   go install github.com/deepmap/oapi-codegen/v2/cmd/oapi-codegen@latest
   ```

3. **生成代码**
   ```bash
   make generate
   ```

4. **开发模式运行**
   ```bash
   make run-dev
   ```

5. **运行测试**
   ```bash
   make test
   ```

6. **代码检查**
   ```bash
   make lint
   ```

---

## 📊 测试覆盖

### 覆盖率目标

- **总体覆盖率**: 70%
- **核心模块**: 80%+
- **框架组件**: 90%+

### 覆盖率报告

- **本地**: `coverage.html`
- **CI/CD**: Codecov 集成
- **阈值检查**: CI 中自动检查

---

## 🚀 快速开始

### 1. 安装依赖

```bash
make deps
```

### 2. 安装开发工具

```bash
# Air (热重载)
go install github.com/cosmtrek/air@latest

# Wire (依赖注入)
go install github.com/google/wire/cmd/wire@latest

# Ent (ORM)
go install entgo.io/ent/cmd/ent@latest

# oapi-codegen (OpenAPI 代码生成)
go install github.com/deepmap/oapi-codegen/v2/cmd/oapi-codegen@latest
```

### 3. 生成代码

```bash
make generate
```

### 4. 开发模式运行

```bash
make run-dev
```

---

## 📝 注意事项

1. **框架性代码**：本项目是框架代码，不包含具体业务逻辑
2. **统一接口**：所有组件使用统一的接口，便于替换和测试
3. **文档优先**：每个组件都有详细的文档和示例
4. **测试覆盖**：保持高测试覆盖率，确保代码质量
5. **示例代码**：示例代码位于 `examples/framework-usage/`，不是框架的一部分

---

## 🔗 相关文档

- [API规范与代码生成](01-API规范与代码生成.md)
- [框架使用示例说明](02-框架使用示例说明.md)
- [架构说明](../architecture/00-架构模型与依赖注入完整说明.md)
- [开发指南](../guides/development.md)
- [CI/CD 配置](../../.github/workflows/ci.yml)

---

**最后更新**: 2025-01-XX
