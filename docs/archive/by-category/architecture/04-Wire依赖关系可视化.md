# Wire 依赖关系可视化

> **版本**: v1.0
> **日期**: 2025-01-XX

---

## 📊 依赖关系图

### 1. 基础依赖关系图

```text
┌─────────────────────────────────────────────────────────────┐
│                    Wire 依赖关系图                            │
└─────────────────────────────────────────────────────────────┘

Config (配置)
  │
  ├─→ Database (数据库)
  │     │
  │     ├─→ UserRepository (用户仓储)
  │     │     │
  │     │     └─→ UserService (用户服务)
  │     │           │
  │     │           └─→ HTTPRouter (HTTP 路由)
  │     │                 │
  │     │                 └─→ App (应用)
  │     │
  │     └─→ OrderRepository (订单仓储)
  │           │
  │           └─→ OrderService (订单服务)
  │                 │
  │                 └─→ HTTPRouter
  │
  ├─→ Cache (缓存)
  │     │
  │     └─→ UserService
  │
  └─→ MessageQueue (消息队列)
        │
        └─→ OrderService
```

### 2. 完整架构依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│              完整架构依赖关系图（Clean Architecture）          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Interfaces Layer                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ HTTP Router  │  │ gRPC Server  │  │ GraphQL      │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                 │               │
└─────────┼─────────────────┼─────────────────┼───────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │
┌───────────────────────────┼───────────────────────────────┐
│                    Application Layer                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ UserService  │  │ OrderService │  │ EventBus     │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │             │
└─────────┼─────────────────┼─────────────────┼─────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │
┌───────────────────────────┼───────────────────────────────┐
│                    Domain Layer                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ User Entity  │  │ Order Entity │  │ Repository   │    │
│  │ Repository   │  │ Repository   │  │ Interface    │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │             │
└─────────┼─────────────────┼─────────────────┼─────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │
┌───────────────────────────┼───────────────────────────────┐
│                 Infrastructure Layer                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  Database    │  │    Cache     │  │ MessageQueue │    │
│  │  (Ent/PostgreSQL) │  (Redis)   │  │  (Kafka)     │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │             │
└─────────┼─────────────────┼─────────────────┼─────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │
                    ┌───────┴───────┐
                    │    Config     │
                    └───────────────┘
```

### 3. Wire Provider 依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                  Wire Provider 依赖关系图                     │
└─────────────────────────────────────────────────────────────┘

InitializeApp(cfg *Config)
  │
  ├─→ NewConfig() → *Config
  │
  ├─→ NewDatabase(cfg) → *Database
  │     │
  │     ├─→ NewUserRepository(db) → UserRepository
  │     │     │
  │     │     └─→ NewUserService(repo) → *UserService
  │     │           │
  │     │           └─→ NewHTTPRouter(service) → *Router
  │     │                 │
  │     │                 └─→ NewApp(router) → *App
  │     │
  │     └─→ NewOrderRepository(db) → OrderRepository
  │           │
  │           └─→ NewOrderService(repo) → *OrderService
  │                 │
  │                 └─→ NewHTTPRouter(service)
  │
  ├─→ NewCache(cfg) → *Cache
  │     │
  │     └─→ NewUserService(repo, cache) → *UserService
  │
  └─→ NewMessageQueue(cfg) → *MessageQueue
        │
        └─→ NewOrderService(repo, mq) → *OrderService
```

---

## 🗺️ 依赖关系矩阵

### 1. Provider 依赖矩阵

| Provider | 依赖类型 | 依赖来源 | 被依赖 | 复杂度 |
|----------|---------|---------|--------|--------|
| `NewConfig` | 无 | - | Infrastructure | ⭐ |
| `NewDatabase` | `*Config` | Config | Domain | ⭐⭐ |
| `NewCache` | `*Config` | Config | Application | ⭐⭐ |
| `NewMessageQueue` | `*Config` | Config | Application | ⭐⭐ |
| `NewUserRepository` | `*Database` | Infrastructure | Application | ⭐⭐⭐ |
| `NewOrderRepository` | `*Database` | Infrastructure | Application | ⭐⭐⭐ |
| `NewUserService` | `UserRepository`, `*Cache` | Domain, Infrastructure | Interface | ⭐⭐⭐ |
| `NewOrderService` | `OrderRepository`, `*MessageQueue` | Domain, Infrastructure | Interface | ⭐⭐⭐ |
| `NewHTTPRouter` | `*UserService`, `*OrderService` | Application | App | ⭐⭐ |
| `NewApp` | `*Router` | Interface | - | ⭐ |

### 2. 层次依赖矩阵

| 层次 | 依赖层次 | 被依赖层次 | 依赖方向 |
|------|---------|-----------|---------|
| **Interfaces** | Application | - | ↓ |
| **Application** | Domain, Infrastructure | Interfaces | ↓ |
| **Domain** | - | Application | ↓ |
| **Infrastructure** | Config | Domain, Application | ↓ |
| **Config** | - | Infrastructure | ↓ |

---

## 📈 依赖复杂度分析

### 1. 依赖深度

```
深度 0: Config
深度 1: Database, Cache, MessageQueue
深度 2: UserRepository, OrderRepository
深度 3: UserService, OrderService
深度 4: HTTPRouter
深度 5: App
```

### 2. 依赖广度

```
Config → 3 个依赖（Database, Cache, MessageQueue）
Database → 2 个依赖（UserRepository, OrderRepository）
UserService → 1 个依赖（HTTPRouter）
HTTPRouter → 1 个依赖（App）
```

### 3. 循环依赖检测

```
✅ 无循环依赖
所有依赖都是单向的，从上层到下层
```

---

## 🔄 依赖注入流程时序图

```
┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
│  Wire   │  │ Config  │  │Database │  │Service  │  │  App    │
└────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
     │            │            │            │            │
     │ InitializeApp(cfg)      │            │            │
     ├───────────>│            │            │            │
     │            │            │            │            │
     │            │ NewConfig()│            │            │
     │            ├───────────>│            │            │
     │            │<───────────┤            │            │
     │            │            │            │            │
     │            │ NewDatabase(cfg)        │            │
     │            ├───────────>│            │            │
     │            │<───────────┤            │            │
     │            │            │            │            │
     │            │            │ NewRepository(db)       │
     │            │            ├───────────>│            │
     │            │            │<───────────┤            │
     │            │            │            │            │
     │            │            │            │ NewService(repo)
     │            │            │            ├───────────>│
     │            │            │            │<───────────┤
     │            │            │            │            │
     │            │            │            │            │ NewApp(service)
     │            │            │            │            ├───────────>│
     │            │            │            │            │<───────────┤
     │            │            │            │            │            │
     │<─────────────────────────────────────────────────────────────────┤
     │            │            │            │            │            │
```

---

## 🎯 依赖优化策略

### 1. 减少依赖深度

**策略**：使用接口解耦，减少直接依赖

```go
// ❌ 深度依赖
App → Router → Service → Repository → Database → Config

// ✅ 优化后（使用接口）
App → Router → Service → Repository(interface) → Database → Config
```

### 2. 减少依赖广度

**策略**：使用 Provider 集合组织相关依赖

```go
// ❌ 分散的依赖
wire.Build(
    NewDatabase,
    NewUserRepository,
    NewOrderRepository,
    NewProductRepository,
    // ... 更多
)

// ✅ 使用 Provider 集合
var RepositoryProviderSet = wire.NewSet(
    NewUserRepository,
    NewOrderRepository,
    NewProductRepository,
)
wire.Build(
    NewDatabase,
    RepositoryProviderSet,
)
```

### 3. 避免循环依赖

**策略**：设计单向依赖，使用接口解耦

```go
// ❌ 循环依赖
Service → Repository → Service

// ✅ 单向依赖
Service → Repository(interface) → Database
```

---

**最后更新**: 2025-01-XX
