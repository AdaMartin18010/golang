# Secret 示例文件
# 注意：实际使用时需要创建真实的 Secret，不要提交敏感信息到版本控制

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: default
type: Opaque
stringData:
  url: "postgres://user:password@postgres-service:5432/dbname?sslmode=disable"

# 创建 Secret 的命令：
# kubectl create secret generic db-secret \
#   --from-literal=url=postgres://user:password@postgres-service:5432/dbname?sslmode=disable
